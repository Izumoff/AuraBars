#pragma once

// No Win32 dialog resources remain in this project - the settings dialog
// is built entirely from AIMP's native IAIMPUIForm/IAIMPUIControl widget
// framework (see OptionsFrame.cpp), which needs no resource-compiler IDs.
